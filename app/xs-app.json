{
    "welcomeFile": "/index.html",
	"authenticationMethod": "none",
	"logout": {
		"logoutEndpoint": "/do/logout"
	},	
    "routes": [	 
	   
        {
            "source": "/v2/(.*)$",
      		"target": "/v2/$1",
      		"authenticationType": "none",
			"destination": "srv_api"
		},  
		{			
			"source": "^/(.*)$",
			"target": "$1",
			"localDir": "./"
		}
    ]
}